# Changelog

All notable changes to dsh-remote are documented here. This project follows
[Semantic Versioning](https://semver.org/).

## [0.6.15] - 2026-09-25

> 补丁版，主题是**「用户报的五件事」**——两条 0.6.14 自身引入/未覆盖的硬伤（Linux/NAS 起不来、
> 目录选择器反而把插件装挂），加三项体验修复（慢、微信回话错会话、手机传不了图）。
> 全部来自用户群反馈 + 诊断日志，逐条有测试钉住。

### ① 飞牛 fnOS / Linux：bridge **从来没被拉起来过**（最严重）

用户诊断（插件 0.6.14，平台 linux）：

```
连接阶段: starting（正在启动 Bridge…）   bridge 进程: 未运行   后台守护: 未运行
上游 dsh web: http://127.0.0.1:3080（默认值 3080（未取到 dsh web 实际监听端口），⚠️ 不可达）
安装日志: [dsh-remote-web] 自动启动 bridge 未成功(unsupported): 当前平台（linux）暂不支持自启动服务
```

- **根因 1**：`startBridge()` 只认 macOS 的 launchd，Linux 直接返回 `unsupported`。
  而 Linux 既不是 darwin 也不是 win32 → 自愈每轮都被挡回，**bridge 永远起不来**。
  用户看到「端口不对」于是反复改端口（改成 20xx、又改回 3080）—— 其实**根本没人在起进程**，
  跟他改的那个端口毫无关系。
- **根因 2**：自愈的就绪判据只对 Windows 放行，Linux 即使拉起来也会每轮重复"写 unit + 起进程"。
- 修法：新增 `startBridgeLinux()` 阶梯 —— 有 user systemd 会话就用 `systemd --user`
  （开机自启 + 崩溃自愈），**没有就退到脱离进程**（NAS/容器是常态，与 Windows 同一条路径）；
  `serviceManager` 也如实上报 `systemd` / `detached`（以前一律写 systemd，面板文案跟着错）。
  `dsh-setup.mjs` 的 Linux 重启路径同样补上脱离进程兜底。

### ② 上游端口发现：NAS 上 dsh web 在 2298，五条路径全都没命中

- 上面那个用户的 dsh web 实际在 **2298**，而 `ctx.webServer.port` 在该环境下取不到、
  NAS 上 `lsof`/`pgrep` 常常没装、静态候选只有 3080 与 Desktop 的 43120 区间 → **2298 永远不在候选里**。
- 修法一：`upstream-discovery.mjs` 新增「**本机所有回环 LISTEN 端口**」候选（源标注 `listen`），
  多来源合并：Linux 直接读 `/proc/net/tcp{,6}`（零依赖，NAS 上最可靠）+ `ss` + `lsof` + `netstat`。
  排在静态候选**之前**（排后面等于白加）；仍逐个做身份校验，陌生服务一律拒。
- 修法二：身份校验新增**第二判据** —— 本插件自己的 `GET /dsh-remote/self`。
  首页是登录页/自定义门户时（`__ModuleLoader__` 等特征一个都不出现），端口再对也会被判"陌生服务"；
  而只有宿主这台 web server 会应答我们的路由，这是本机最硬的同一性证据。
- 修法三：插件半新增 `discoverUpstreamSelf()` —— 问不到自己端口时主动发现一次并固化到
  `.dsh-upstream`（watcher/bridge 是独立进程，只认这个文件），面板与 spawn 都随之用上正确地址。

### ③ 目录选择器：0.6.14 的"修法"反而让插件**装载失败**（服务名冲突）

用户诊断（DSH Desktop）：

```
error: service "directoryPicker" has been registered at .dLateDirectoryPicker3
  at new DirectoryPicker (…/dsh-host-directory-picker/lib/index.js:1782)
  at new BrowseDirectoryPicker (…/dsh-host-directory-picker-browse/lib/index.js:245)
```

- 根因：`native` 与 `browse` 两个后端注册的是**同一个服务名** `ctx.directoryPicker`
  （seam 文档：one implementation per context, loading a second throws）。
  所以 0.6.14 的「先建 browse、后摘 auto」**必然**抛 duplicate-service —— 顺序写反了。
- 修法：**先摘 auto（释放服务名）→ 再建 browse → 建不起来就回滚**：卸掉半成品，
  把 auto 条目**按原 id** 建回来（用户至少还有原生可用）。另加"摘不掉就一根手指都不动"的护栏。
- 测试同步重写：假 loader 现在会**复现 duplicate-service**，把"顺序反过来必炸"这条钉成回归锁。

### ④ 打开慢：同一条响应被 base64 编了**三层**（1.333³ ≈ 2.37×）

用户实测（dev-1014f947f8ab，Ubuntu 上的 dsh + Windows 访问，首屏等几分钟）：
gzip 后 5.35 MiB 的聚合包，经三层 base64 变成 bridge→中继 12.69 MiB、手机下载 9.52 MiB。

| 层 | 位置 | 处理 |
|---|---|---|
| ① gzip 正文 → base64 进明文 JSON | `e2ee-client.mjs` | **去掉**（改成二进制框架，客户端声明后启用） |
| ② AES-GCM 密文 → base64url 进信封 JSON | `e2ee-client.mjs` | 保留（JSON 装不下字节，且它承载手机侧解密） |
| ③ 整个信封 JSON → 再 base64 进隧道帧 | `dsh-bridge.mjs` | **去掉**（中继拿到就解，纯属白花 33%） |

- ③ 是纯浪费：中继收到帧后第一件事就是解回 UTF-8 再写给手机。现在直接发 UTF-8
  （`bodyBase64:false`；中继两种形态都支持）。**这条腿正是按流量计量、用户被卡的那条**。
- ① 用「头 JSON + 正文原始字节」的二进制框架（首字节 `0x02`，与 JSON 的 `{`=0x7B 永不冲突），
  但只对**在请求里声明** `x-dsh-e2ee-want: bin` 的客户端启用 —— 镜像页 shim 声明，
  老客户端（`native.html` 的 WC-CORE / 企业版内置那份）不声明就仍走旧 JSON，**零兼容风险**。
  该头是端到端内部协商头，bridge 会**摘掉它再转发上游**（它不是业务头）。
- 效果：电话侧下载 9.52 → 7.14 MiB（1.78× → 1.33×），bridge→中继 12.69 → 7.14 MiB。
- 协议文档 `docs/e2ee-protocol.md` 同步更新（两种明文形态 + 协商规则 + 回包帧体不再套 base64）。

### ⑤ 微信通道：回话发进了**上一次**的会话

业主实测：B 任务跑完推了结论，他在微信里直接回「再改一下」，那条消息却发给了**上一次的当前会话 A**
—— A 平白多了一条指令，B 永远收不到。
修法：**完成推送把「当前会话」切到刚跑完的那个**（并落盘）。语义即"最近完成的任务 = 最近一次推送的
对象 = 你现在回话的对象"，这是唯一不会让人踩空的解释。已是当前会话时不重复写状态。

### ⑥ 手机端传不了图 / 传了模型看不见

官方只把 `image/png|jpeg|webp|gif` 当**图片**（本地编码随 prompt 发给模型）；
其余一律当**普通文件**后台上传，模型只拿到一个文件引用 —— 用户看到的就是"识别不了图片"。
而手机恰恰最容易产出非白名单类型：iPhone「高效」格式 = `image/heic`；部分 Android 图库给**空 type**。

- 修法：适配层在**捕获阶段**拦下官方隐藏 `input[type=file]` 的 `change`（必须早于 React 根容器），
  把这类文件**无损改 MIME**（扩展名可判定时，如空 type + `.png`）或 canvas 转 JPEG 后
  替换 `input.files` 并**重新派发**；官方认得的类型一个字节都不碰。
- 任何失败（浏览器解不了 HEIC / 没有 DataTransfer / canvas 不可用）都**原样重新派发**，
  绝不吞掉事件、绝不丢文件 —— 最差也只是回到今天的行为。

### 测试

- 新增 `packages/dsh-remote-web/test/linux-bridge.test.mjs` 7 条（无 systemd 时必须真的派生守护、
  有 systemd 时写 unit 且带 `DSH_BRIDGE_UPSTREAM`、候选端口里必须有 2298 且排在静态候选之前、
  `/dsh-remote/self` 第二判据、/proc 解析、静态接线护栏）。
- 新增 `clients/dsh-remote/test/mobile-adapter-image.test.mjs` 7 条（HEIC 转码、空 type 无损改 MIME、
  白名单零行为、解码失败/无 DataTransfer 时原样重发、混合选择保序）。
- 重写 `packages/dsh-remote-web/test/picker-pin.test.mjs`（12 条，含"顺序反过来必炸"的回归锁）。
- `e2ee-client` / `e2ee-shim` / `e2ee-bridge` / `wechat-runtime` 均补了钉住本次结论的用例
  （二进制明文框架、协商头不转发上游、隧道帧不再套 base64、完成推送切会话）。
- 顺手修掉一个测试设施真 bug：桥接对拍用的假上游按分片 `body += chunk`，
  多字节 UTF-8 跨 64KB 分片会被拆坏（加大正文用例才暴露）。

### 同版一并

- 诊断文案：上游不通时不再建议"把 dsh web 重启在 3080"（对 NAS 用户是错的方向），
  改为"会自动发现真实端口；仍不通可设 `DSH_BRIDGE_UPSTREAM=<你的端口>`"。
- `decodeHttpRequestPlain()` 之前对 `Uint8Array` 用 `String(buf)` → 永远"不是 JSON"，
  现在按字节解码（对拍用例抓到的真问题）。

## [0.6.14] - 2026-09-23

> 补丁版，主题是**「端口不是 3080 就用不了」**——一类环境兼容问题，不是某个用户的特殊情况。

### 根因：上游端口被写死成 3080

- 桥接链路两端都把上游钉在 `http://127.0.0.1:3080`（独立 `dsh web` 的默认端口）：
  - **watcher**（`dsh-setup.mjs run`）用 3080 做「dsh web 在线吗」的判据，**在线才派生 bridge**；
  - **bridge**（`dsh-bridge.mjs`）只认环境变量，缺省同样回退 3080。
- 于是只要宿主端口不是 3080 —— `dsh web --port 8090`、`--port 0`（系统分配）、
  或 **DSH Desktop**（Electron 壳 `dsh-plugin-desktop`，默认 **43120**、被占用还会 +1）——
  watcher **永远探不到**，bridge **永远不会被派生**，而面板只会停在「正在启动 Bridge…」：
  ```
  连接阶段: starting（正在启动 Bridge…）   bridge 进程: 未运行   中继注册: 未注册
  ```
  用户侧看到的是"插件坏了"，实际是我们把端口写死了。

### 修法：动态发现 + **身份校验**（watcher 与 bridge 共用同一份实现）

新增 `clients/dsh-remote/upstream-discovery.mjs`（单一实现，两端共用），发现顺序：

1. `DSH_BRIDGE_UPSTREAM`（显式覆盖，部署/排障用；直采信，保持既有语义）；
2. `<relayDir>/.dsh-upstream`（**插件半**用 `ctx.webServer.port` 落盘，最权威）；
3. `DSH_WEB_URL` 的端口；
4. **实测端口**：本进程祖先进程 / 本机 dsh 进程正在 LISTEN 的端口（lsof / netstat）；
5. 静态候选：3080 + DSH Desktop 的 43120 及其递增区间。

- **候选必须通过 dsh web 身份校验**：43120 这类端口可能被别的本地程序占用，
  只看"端口有响应"就把 bridge 接上去，既不可用也可能把用户流量交给陌生服务。
  判据取 dsh web 自身特征（`__ModuleLoader__` / `DeepSeek Harness` / `dsh web authentication required` 等）。
- watcher：探测失败会**主动做一次动态发现**再试，并把发现到的地址经 `DSH_BRIDGE_UPSTREAM`
  传给 bridge；启动日志不再谎称"等待 3080"。
- bridge：启动时先确定上游再连隧道；上游请求失败时**带冷却地重新发现**
  （宿主换端口 / dsh web 重启到别的端口都能自愈，不必重启 bridge）。

### 同版一并修：Windows「添加工作区」按钮没反应

- 现象：镜像页点「添加工作区」无效（Mac 正常）；根因是**目录选择器**选错了那一支——
  官方 `directory-picker-auto` 只在「绑定非回环」时才用**浏览器内**选择器，否则 darwin/win32 一律挂
  **系统原生**对话框。原生那个框弹在**电脑那台机器的屏幕上**，拿手机的人什么也看不到，表现就是"点了没反应"；
  Windows 那条原生链路还额外依赖 `koffi` 原生模块 + 一个 worker 子进程（要合成 Alt 抢前台），失败点更多。
  DSH Desktop（Electron 壳）自带 webserver 配置（实测监听 43120 / 回环），我们的 LAN 绑定补丁不生效，
  于是一直走原生那条路。
- 修法：插件主动把选择器**固定成浏览器内实现**（官方文档给的"直接合成那一对"的钉法）：
  先在 loader 里建起 `dsh-host-directory-picker-browse` + `dsh-client-ui-directory-picker-browse`，
  **成功之后**才摘掉 auto 条目（它的析构会连带卸掉原生那对）。
- 三条护栏（顺序错一条就会把用户的选择器搞没）：① 已经是 browse 就完全不碰；
  ② 先建 browse、后摘 auto —— 建不起来就原样保留原生；③ 任何失败只记日志，绝不把 dsh web 拖挂。
  想保留系统原生对话框的人可设 `DSH_REMOTE_NATIVE_PICKER=1` 退出。
- 诊断面板新增一行「目录选择器」：是原生时会直接说明"远程时对话框弹在电脑那台机器上"，
  这类"按钮没反应"下次一眼可定位。

### 同版一并做：镜像页首屏加载提示

- 现象：镜像页首屏要拉 dsh web 的全部客户端插件（实测 ~14 MB 的**不可拆分**聚合请求），
  慢的时候页面上**只有官方那个转圈**，用户无法区分"在加载"和"卡死了"，只能反复刷新——反而更慢。
- 做法：适配层在**页面解析的第一时间**就挂一条人话提示（早于所有主机判定，因为首屏慢的正是判定等不到的时候）：
  「正在加载远程桌面…」+ 秒表（已等待 N 秒）+ 分阶段补充说明（8 秒后"可能要一两分钟，会自动进入"、
  30 秒后"可以下拉刷新重试"）。官方 UI 一就绪就自动撤下，另有 4 分钟兜底与 pagehide 清理。
- 边界：提示层 `pointer-events:none` + 半透明，**绝不挡住任何点击**；不限定窄屏（Windows/桌面浏览器同样要等）。

### 测试

- `upstream-discovery` 11 条：提示优先级、规范化、身份校验（拒绝陌生服务/5xx/空响应）、
  候选命中与来源标注、Desktop 端口区间、找不到时回退 3080、真实监听端口解析。
- **watcher 级行为回归 2 条**（用假 launchctl/pgrep/ps 隔离宿主）：
  ① 上游在非默认端口时 watcher 必须发现并派生 bridge，且**派生的 bridge 拿到的正是那个端口**；
  ② 上游确实不可达时保持等待，不谎报在线、不派生。
- `test:bridge` 388 通过 / 0 失败。

## [0.6.13] - 2026-09-23

> 补丁版，主题是**「打开慢、转圈两分钟」的根治**，外加三个真实报错。起因是一条诊断：
> 同一个地址，免费档打开要**两分钟**，升到 PRO 就"好很多"。

### 根因不是带宽，也不是服务器：是限速撞上了一类超大聚合请求

- **实测**：手机镜像页首屏 **14.85 MB**，其中 `dsh web` 把几十个客户端插件合成的
  **单个** `/plugins/??…` 请求就占 **13.4 MB**（一个请求、无法并行、无法拆分）。
- 而免费档限速 **1 Mbps（≈128 KB/s）** → 这一个请求就要 **100+ 秒**；仓库文档里本来就写着
  「1 GB 免费额度 ≈ 4.1 小时使用」，用户看到的就是"转圈两分钟"。
- 同一份数据里还有第二个洞：`bridge` 把上游已经 gzip 好的响应**原样按未压缩发出**
  （上游 5.12 MB → 隧道里 13.39 MB，**2.6 倍**），首屏时间被白白乘了一次 2.6。

### 改法一：档位速率**完全不变**，加一层**隐藏的后置放行**

- 服务端新增「超大请求门禁」：单个响应被限速**卡满 N 秒**后，剩余字节**一次性放行**。
  默认 **免费 20 秒 / PRO 10 秒 / Pro Max 5 秒**（免费档本来就最慢，给最宽）。
- 于是：**普通请求完全感受不到差异**（分档速率、`/_quota` 下发的 `max_mbps` 一字未改），
  只有那种"怎么等也等不完"的超大聚合请求，从**无界等待**变成**有界等待**。
- 阈值是**我方后台可配**的隐藏参数：它经**内部端点**（`GET /api/internal/router-config`，
  需 `DSH_INTERNAL_TOKEN`）下发给中继，**不进** `public-config`——匿名可读的响应里
  一个字都不会出现（有端到端用例守着）。
- 计量不受影响：放行只改**节奏**，字节仍然全额计入月流量。

### 改法二：把压缩真正打开（隧道 + nginx 两层）

- **bridge 不再丢弃上游压缩**：`undici` 解压后仍保留 `content-encoding: gzip` 响应头，
  旧代码据此认为"body 已编码"而放弃重压 —— 现在按"手上这份是明文"处理，
  13.39 MB 的包回到 5.12 MB（**省 2.6 倍**）。这条有专门的回归用例（上游真 gzip 的场景）。
- **nginx 开 gzip**（HTML/JS/CSS/JSON/manifest/SVG）。刻意**不**用 `gzip_types *`：
  E2EE 密文不可压，且 `text/event-stream` 一旦被压就会攒块输出，把下面的流式转发废掉。

### 改法三：SSE 真正流式（修掉 `/plugins/events` 挂 120 秒）

- `dsh web` 的 `/plugins/events`（HMR 事件通道）是**永不结束**的流，而旧协议只有
  "一个完整响应"帧 → bridge 只能挂到自己 **120 秒**上游超时才报错，浏览器 `EventSource`
  又立刻重连：**Network 里永远有一条转圈的请求**。现在头部先回、字节逐块推、
  手机断开还会通知 bridge 掐掉上游（不留孤儿连接）。
- **能力协商保证两段链路可独立升级**：中继在注册 ack 里声明 `caps: ["http-stream"]`，
  bridge 拿不到就老实退回旧的整包缓冲行为 —— 旧中继 + 新 bridge **不会**从"挂 120 秒"
  退化成"秒断狂重连"。

### 三个真实报错

- **`/manifest.webmanifest` 404**：`dsh web` 的 HTML 带 `<base href="/">`，所以它永远解析到
  **根路径**；而浏览器抓 PWA manifest 按规范**不带凭据**，根路径就解析不到设备 → 404。
  现在 manifest 与图标由中继**本地应答**（内容本来与设备无关，`start_url` 指向手机外壳），
  「添加到主屏幕」能拿到名字与图标；白名单之外仍是 404。
- **慢网下左边抽屉按钮出不来**：适配层原先只在启动后 `[0.7s/1.6s/3.2s/7s/12s]` 试五次，
  而官方 SPA 的 bundle 在慢网下几十秒才挂载 → 五次全耗尽 → **按钮永远不创建**。
  现在改成"一直等到就绪"（MutationObserver + 低频兜底轮询），并加常驻看门狗：
  官方重新渲染把按钮弄丢也会补回来。**不管网络多慢，抽屉都出得来。**
- （`/plugin-center/checkUpdates` 的 405 **不在本版**：那是 `@max-null/dsh-plugin-center`
  ≤0.2.19 与 DSH 0.1.5-rc.2 的兼容问题——它用 `rpc.handle` 注册通道，而该版本 web 组合下
  会抛 `cannot get property "webServer" without inject`，路由没注册 → 落到静态兜底 → 405。
  上游 0.4.1 已改用 `fetch.register` 修掉，升级插件即可。）

### 测试

- 新增用例：门禁行为（有界等待 / 关闭时逐字节等价 / 免费档 13.4MB 从 100+ 秒收敛到阈值附近）、
  门禁**不得**出现在任何用户可见响应、上游 gzip 回归、SSE 流式端到端（逐块到达 + 断开掐上游）、
  manifest 兜底（含"白名单外仍 404"）、抽屉"第 15/60 秒才出现"回归。

## [0.6.12] - 2026-09-23

> 补丁版，主题是**Windows 装机/连接失败**。起因是一条真实用户反馈（Windows、0.6.10）：
> 「手机和电脑登录同一个账号，但手机显示还没有绑定任何电脑，**多次修复都没有解决**」。
> 生产遥测同期数据：近 14 天 Windows **427 台**机器里注册成功只有 **152 台（35.6%）**，
> 装机失败 **138 台（32.3%）**，`bridge_started` 却从未注册 **52 台（12.2%）**；
> 且 **60.4% 的机器还停在 v0.6.8 及更早** —— 而老版本的「一键更新」本身就是坏的
> （157 台装机失败且从未连上的机器里，79 台点过一键更新、79 台全部 `update_failed`）。

### 装不上的根因：运行环境不再依赖 npx
- **运行环境随插件分发**：插件包现在自带 `runtime/`（`dsh-setup.mjs` + `clients/` + `ws`，发版时由
  `scripts/bundle-runtime.mjs` 打进 npm 包与 Release 资产）。补装从「后台跑 npx 下载」变成**本地拷贝**：
  零网络、零 npx、零控制台代码页问题；运行环境版本**永远等于插件版本**（不再出现插件 0.6.10 配运行时 0.6.9）。
  拷贝后**自检**（入口脚本点名的文件必须都在），半装绝不当作成功。
- **半装运行环境能自愈了**：此前「运行环境是否就绪」只判 `<relayDir>/dsh-setup.mjs` **存在** ——
  存在 ≠ 能加载。npx 补装中途被打断（关窗口 / 杀软锁文件 / 无输出看门狗结束进程）会留下
  「入口在、它 import 的 `clients/...` 不在」的半装运行时，插件却报「已就绪」，于是插起来的守护**秒退**
  （`Cannot find module`），而自愈从此永久停摆。现在按「入口脚本点名的文件真的都在」判定，缺失即整份重拷。
- **非 npx 的更新通道**：`npx` 两次都不成功时，自动改用 **HTTPS 取 npm tarball + 纯 Node 解包**
  （`lib/offline-update.mjs` + `lib/tarball-update.js`，零第三方依赖，含路径穿越防护）。这是给
  「npx 在 Windows 上跑不起来」那批机器留的出路。

### 连不上的根因：守护与上游不再「无人知晓」
- **守护进程（watcher）活着却拉不起 bridge 时，现在真的会重启它**：此前 `startBridge` 只要读到
  `.dsh-watcher.pid` 指向存活进程就返回「已在运行」，于是面板的「启动 / 立即重试 / 一键修复」与每轮自愈
  **全是空操作** —— 这正是用户那句「多次修复都没有解决」。现在：守护存活超过阈值仍无 bridge 进程 →
  结束它再拉起（次数有上限，正常启动窗口内不打扰，服务端登录限流期间不误判）。
- **上游端口不再写死 3080**：插件本来就知道 dsh web 的真实监听端口（`mintHarnessCookie` 用它给会话 cookie
  写 authority），但 bridge/watcher 一侧全部写死 3080。`dsh web --port 8090` 或 `--port 0`（系统分配空端口）时，
  watcher 的「dsh web 在线吗」探测永远失败 → **bridge 永远不启动**，而面板只会说「正在启动 Bridge…」。
  现在上游地址取自 dsh web 实际端口，透传给 watcher/bridge，并落盘 `.dsh-upstream` 供开机自启的守护读取。
- **诊断信息说清卡在哪**：新增「后台守护态（pid/存活时长/是否卡死/自动重启次数）」「上游地址与可达性
  （含来源）」「运行环境缺哪些文件」「登录限流剩余时间」，并附上 **bridge 与安装日志的末尾**（手机号、令牌、
  密码一律先脱敏）—— 此前只有日志**路径**、「最近错误: 无」，问题只能靠来回问。

### 用户自己能报、自己能修
- **「上报故障给开发者」（用户主动、可预览）**：面板可展开**将要发送的每一个字**，用户可写描述、可取消
  附带日志，**点确认才发**；走既有反馈通道（正文 1 条 + 日志/报告作为同一条反馈的回复分片），
  不需要服务端部署。**绝不后台自动上传日志**。
- **「让 DeepSeek 帮我修」**：面板给一段可一键复制的提示词（含本机路径、上游地址、当前阶段），
  用户在自己电脑的 DeepSeek Harness 里跑，由本机 agent 排查修复并产出**固定格式的结论报告**
  （症状 / 机制判定 / 关键证据 / 已做的修复 / 修复结果 / 仍需人工处理），用户可把报告回传给我们。
  这样不必等我们发版就能自救，我们也拿到可解析的现场证据。

### 修复
- 半装运行环境会被判为未就绪并自动补装（见上），面板文案如实说「不完整（缺 X、Y）」而不是含糊的「安装中」。
- 上游不可达时把原因写进面板提示（此前这一段是空白）。
- 上报/回传的请求体上限 256KB（超限整体读完再回 413，避免客户端等一个没被读完的请求）。

## [0.6.11] - 2026-09-23

> 补丁版。修业主实测的「**每日简报推送了两次**」，外加一处偶发把解绑流程打挂的 WebSocket 关闭问题。

### 修复
- **日报重复推送**：`#maybeDigest` 是「读 state → 等网络发送 → 写 state」的**跨 await 读-改-写**。发送要等网络，这段窗口里第二个执行者会读到"今天还没发" → **两条都发出去**。两个真实撞车场景：① 更新期间新旧 bridge 短暂共存；② 一次发送超过 60s，下一次 tick 叠上来。
  现在改为 `openSync(..., "wx")`（不存在才创建，内核保证原子）**占位**：同一时刻只有一个执行者拿得到。占位发成功就留着（比 state 更强，重启/换实例都不会丢）；发失败立刻释放，留给下一 tick 补发；超过 10 分钟视为陈旧（进程在发送途中被杀）会接管 —— 宁可极小概率重发一次，也不要出现"那天彻底不发了"。
- **偶发「解绑」打挂**：`EventSubscriber.close()` 先 `removeAllListeners()` 再 `close()`，而**握手中**的连接被关闭时 ws 会**异步** emit `error` —— 没有监听者就成了未捕获错误。现在握手中的连接走 `terminate()`，并先挂一个吞掉的 error 监听。

## [0.6.10] - 2026-09-23

> 正式版（`latest`）。本版把「免费/付费权益」做成**后台可配置的权限包**、补齐管理后台能力，
> 并修掉一批真机问题。README 同时做了精简：版本历史统一记在这里，不再堆在首页。

### 权益包：免费/付费权益后台可配、实时生效（无需发版）
- 后台「权益包」页可勾选**每个档位能用什么能力**、改**每月消息额度**；保存即生效，最长 10 分钟内推到用户侧（被拒时会立刻重查）。
- 能力清单只有一份真相源（服务端），客户端以服务端下发的为准，只在内置表保留冷启动兜底 —— **面板展示与微信里的实际判定不可能不一致**。
- 「不多承诺」贯穿始终：付费用户被后台关掉某项能力时说「当前套餐不含」，而不是对着会员说「这是会员功能」；免费用户缺的那一项才归到「会员功能」。帮助文案**逐条按实际能力生成**，不再用"是不是付费档"猜。
- 手机端**升级/续费页**同样展示微信能力差异，数据来自公开配置 —— 后台一改，升级页当页就变。

### 额度与到期提醒
- **额度不足提醒**：以前 `notifyQuotaLow` 是**零调用方的死代码**，永远不会发 —— 用户总是在额度归零那一刻才撞上拒绝。现在用到 80%（或剩余 ≤2 条）自动提醒一次，每个自然月一次，**提醒本身不消耗额度**。
- 会员到期提前 2 天 / 1 天 / 已过期各提醒一次（每阈值每天最多一次，只有真发出去才记账）。

### 微信消息与交互
- 未知指令不再把**会员指令清单**甩给免费用户：回显他打错的那条 + 给**按档位分层**的帮助。
- 提醒（额度 / 会员到期）不再抢占审批的回执位 —— 以前对着最新那条审批回「1」，回执会落到更早的提醒上；顺带修掉一个**从来没生效过**的入口（提醒里的「邀请好友」被当成 DSH 提问派发，必然失败）。

### 管理后台
- **用户管理**：修掉「设为免费用户」不生效（只改了 `users.plan`，权益其实没收回）；新增清空手机号 / 彻底删除。门禁是硬的：目标近期有设备心跳 → 409；`confirm` 必须**逐字一致**，不匹配即拒；彻底删除需显式开启 `ALLOW_USER_PURGE`。
- **公告 / 消息中心**：多条公告、手机端消息气泡与置顶公告（约 3 行截断可点开）、打开设备强制弹出（频率可配一次/每次、支持配图）、UV 与点击上报。

### 修掉的真机问题
- **一条微信指令回两条**：接管时老 watcher 只"请"子进程退出、3 秒就自己 exit，而 bridge 常正卡在 35 秒长轮询里 → 变成孤儿继续轮询同一账号。现在接管时**显式回收**遗留 bridge（先用命令行双重校验再动手），退出前也兜底 SIGTERM→SIGKILL。
- **`/help` 回一坨（无换行无编号）**：旧实现对付费档 `return HELP_TEXT`（**数组**），`String(数组)` 就是逗号拼接。现在帮助逐行生成，且 `reply()` 收到数组一律按行拼接。
- **插件版本号硬编码**，发版漏改时会上报错版本 → 改为从 `package.json` 派生（读不到报 `0.0.0-unknown`，绝不冒充）。
- **`caps: []`（后台明确清空）被当成"没给"** → 回退内置表 = 比后台意图**更多**权限（fail-open）。四处判据统一为「是不是数组」。
- 管理后台网关的审计写在上游结果**之前**（失败的保存/上传也留"成功"痕迹）→ 改为先确认、后留痕，状态码原样透传。
- 数据分析看板 500：`/api/admin/analytics/sources` 引用了未 import 的常量（该端点此前既不在鉴权清单也无任何用例）。

## [0.6.10-beta.7] - 2026-09-22

> 预发版（只发 npm `beta` 通道，`latest` 仍是 0.6.9）。本轮把**微信里收到的东西**整个重做了一遍：
> 排版、审批、日报、权益分层 —— 起因是业主一句「整个格式在微信上没有做过任何兼容，体验很差很差」。

### 微信消息：不再把 Markdown 原样丢给你
- **表格不再是一团竖线**：模型输出的 Markdown 表格会转成逐行「· 列名：值 ｜ 列名：值」。
  （微信**不渲染** Markdown —— 之前你看到的就是字面的 `|` 和 `**`，那不是排版没调好，是根本没转换。）
- **加粗不再显示星号**；**链接保留裸网址**（微信只把裸网址变成可点击链接）；标题变【】、列表统一成 ·、围栏代码只去标记。
- **结论独立成段、保留换行**，不再是挤成一坨的一行。
- **结论上限从 400 字提到 1500 字**（取值闸 700→3000）—— 之前你一条正常的修复总结就被砍掉。
- **「下一步」单独分区**，不再和正文拖在一起。
- **修掉一个真 bug**：旧实现是"整条消息盲切尾部"，而「下一步」恰好拼在最后 ——
  超长时会把**你唯一能照做的那句话**切掉。现在只压正文，头尾永不截断。

### 多条审批：不再"只答了一条，其余变未回应"
- **修掉根因**：回数字原本匹配"最近一条"，而微信是**从上往下读**的 —— 你看着第 1 条回「1」，
  系统答了最后一条，第 1 条永远没人回应，DSH 最终标它「未回应」。现在改为**先进先出**。
- 答完一条会把**下一条重发到底部**，你直接在最新那条上回数字即可（不用往上翻）。
- 通知会明说「你还有 N 条待拍板」，不再让人以为答完就结束了。

### 日报
- **改到晚上 18 点发**（早上 9 点发"今天干了啥"逻辑上就是空的），且一天只发一次。
- **内容是真实的今天回顾**（完成任务 / 出错中断 / 推送条数 / 你拍板次数），不再是兜底的空话。
- 加了一行**克制的品牌露出**。
- **修掉"发送失败也把当天标记为已发"**（当天再也不会补发）。
- **补上从未触发过的会员到期提醒**：`notifyMembershipExpiring()` 之前只有定义、全仓零调用方 ——
  你现在补上了提前 2 天 / 1 天 / 到期当天的提醒（同一阈值一天一次）。

### 权益分层（免费 vs 会员）
- **免费档：能继续已有会话（回复即续接）、能收通知、能拍板、能急停**；
  但**不能开新会话、不能切换会话**。
- **免费档每月 N 条消息额度**（默认 20，可配），用超后引导升级；**审批与指令不吃额度**。
- 能力判定改为**点号前缀授权**，服务端下发的权益包可覆盖内置表 → 后台改权限**无需发版**；
  取不到配置一律**沿用上次**、未知能力**不授权**（绝不因为一次网络抖动把付费用户降级）。
- **面板如实展示"你现在能用什么"**：直接读通道实际在用的一份能力，免费用户能看到锁定的会员项
  （这就是"付费多什么"的对比），并显示本月额度已用多少。有测试逐项比对面板与运行时判定表，防止文案漂移。

## [0.6.10-beta.6] - 2026-09-22

> 预发版（只发 npm `beta` 通道，`latest` 仍是 0.6.9）。本条修的是「自启动服务偶尔整个消失」
> 的**真正根因**（beta.5 修的是同一现象的另一半）。

- **修复：插件写入的自启动配置缺了必需项，导致服务被系统回收后再也回不来**。
  该配置少写了一项「会话类型」声明，于是它无法在正确的系统域里注册、只能退到
  一个在 macOS 26 上**不再支持开机自启与崩溃自愈**的域。结果：服务一旦被系统回收，
  就再也没有任何东西把它拉回来 —— 现场表现正是「自启动服务消失、手动启动也报找不到」。
  现在两个写入方（安装器与插件）的配置模板已对齐，并有测试锁死一致性。
  （附带确认：这次消失是 **macOS 自身的后台任务管理回收**所致，不是本产品误删 ——
  已从系统日志取证。）

## [0.6.10-beta.5] - 2026-09-22

> 预发版（只发 npm `beta` 通道，`latest` 仍是 0.6.9）。本条修两个 macOS 自启动的可靠性问题
> —— 起因是「自启动服务偶尔整个消失，连手动启动命令都报找不到该服务」的排查。

- **修复：macOS 上自启动服务会自己消失**。被系统（launchd）拉起的守护进程，若发现有更早的
  游离守护在跑，会**立刻退出**避让；但监管者把这次秒退当成崩溃并节流，插件的自愈又据此
  认定"服务坏了"，把自启动项摘掉（有时连自启动配置文件一起删）。结果**自启动彻底消失、
  手动启动也报"找不到该服务"**，只能从面板重新启动。
  现在被监管者拉起的实例会**请游离实例让位并接管**；动手前还会先确认那个进程真的是守护进程，
  避免误杀被系统回收的 PID。
- **修复：插件写入的自启动配置漏了系统命令目录**（`/usr/sbin`、`/sbin`）。插件每次自愈/启动
  都会重写同一份配置，于是覆盖掉了安装器里那份正确的 —— 后台进程因此找不到 `ioreg`
  这类系统命令，**机器识别静默退化成按电脑名字识别**（改一次电脑名字就被当成新设备）。
  两处配置现在共用同一条 PATH，并有测试锁死一致性。

## [0.6.10-beta.4] - 2026-09-22

> 预发版（只发 npm `beta` 通道，`latest` 仍是 0.6.9）。beta.3 的分层已可用，
> 本条只修一个 beta.3 引入的可靠性问题（内容并入 beta.3 的分层说明）。

- **修复：每次启动会多登录一次账号**。beta.3 的档位查询没有复用桥接刚拿到的凭据，
  于是同一个启动里出现两条并发的登录请求；而登录接口是**按出口 IP 限流**的
  （15 分钟内 5 次）—— 桥接崩溃重启几次就可能把自己打进限流，**远程访问随之失效**。
  现在启动时直接复用已拿到的凭据，登录次数回到 1 次。

## [0.6.10-beta.3] - 2026-09-22

> 预发版（只发 npm `beta` 通道，`latest` 仍是 0.6.9）。本轮把**微信机器人通道分成两档**，
> 让免费用户用得着、让付费用户有理由付钱，并把免费用户引回远程控制主功能。

### 微信机器人通道：分层

- **免费（通知版）**：任务完成 / 出错 / 停下都会推到微信，能看到会话名与结论；
  需要你拍板时**回一个数字**就完成决定；还能 `/status` 看状态、`/stop` 急停、`/quiet` 免打扰、`/unbind` 解绑。
- **会员**：额外解锁**直接在微信里交代任务**、`/new` 开新任务、`/ls` 看会话列表、
  `/use` 切换会话、`/summary` 重要结论 —— 也就是把"遥控"这一层留给付费用户。
- **免费用户尝试会员能力时**，回复里会说明现在能做什么，并附上**打开 App 的链接**
  （点一下就到设备列表 / 远程控制界面），而不是一句干巴巴的拒绝。
- 完成推送、`/help` 的措辞都**跟着档位走**：不会再对免费用户说"回复就能接着做"
  （他做不到），也不会把会员指令列进"你现在能用的"。
- 试用期由服务端折算成会员档，用户无需额外操作；**自建部署直接是全功能**（不受会员分层约束）。
- 档位取不到时**沿用上次已知档位**：一次网络抖动不会把付过钱的用户降级、也不会让他看到"升级后可用"。
- 面板的「绑定后能做什么」如实写明哪两项免费、哪项是会员能力。

## [0.6.10-beta.2] - 2026-09-22

> 预发版（只发 npm `beta` 通道，`latest` 仍是 0.6.9）。本轮主线是**微信机器人通道**：
> 电脑上的 DSH 可以直接用微信遥控 —— 任务要审批、做完了、出错了都推到微信里，并且能直接回话继续。
> （0.6.10-beta.1 的内容已并入本条；beta.2 补了一处 macOS 机器识别修复。）

### 微信机器人通道（新）

- **扫码绑定一次就好**：面板「微信机器人通道」里扫码绑定，解绑也在同一处。
  绑定状态只上报"绑了 / 解绑了"，不关心此刻在不在线。
- **任务要你审批时推到微信**：可以直接一步回执（允许 / 拒绝），不必再翻回电脑。
- **任务结束推到微信**：带上**会话名称**和**这次做完了什么**；
  停止原因会区分"正常结束"和"出错中断"，不会混成一句"已停止"。
- **在微信里交代任务、切换会话**：`/new` 开新会话（默认沿用最近的项目）、`/ls` 列会话、
  `/use` 挑一个继续、`/stop` 停、`/status` 看状态、`/summary` 要小结；
  直接发一句话就是发给当前会话。
- 另有每日简报、额度不足、会员到期三类提醒。
- 需要先注册登录：未登录时该入口不出现，后台也不会启动通道。

### Windows

- **不再弹出黑色空白命令行窗口**：以前每次登录都会冒出一个黑色窗口，关掉它还会再开；现在完全隐藏。
- **改了电脑名字不再被当成新设备**：改为读取系统里稳定的机器标识，设备列表不会再堆出重复条目。
- 退出时能正常发出"微信通道已下线"的通知（此前 Windows 上这条通知永远发不出去）。
- 取不到当前用户名时，会明确提示配置文件**没有**完成权限加固，而不是默默跳过。

### macOS

- **机器识别也修好了**：以前 bridge 由系统（launchd）拉起时，读机器标识的命令**找不到**
  （系统的 PATH 里没有它所在的目录），于是悄悄退化成"按电脑名字识别"—— 改一次电脑名字就会被
  当成新设备。现在改用绝对路径调用，不再赌环境变量。
  （这与上面 Windows 那条是同一个问题的两面；手机上的既有远程地址不受影响。）

### 远程面板

- 新增「微信机器人通道」标签页：能做什么、怎么绑、绑完长什么样，都在页面里说清楚。
- 营销类入口收进「更多」，首批展示关闭后下沉；首页不再展示会员额度信息。
- 修正若干文案：档位名不再出现在首屏，"更多"里的营销入口不再提具体带宽数字。

## [0.6.9] - 2026-09-19

> 本次更新聚焦两件事：**把日常使用体验打磨好**，以及**把大家反馈的问题逐个解决**。
> （0.6.9-beta.1 ～ beta.3 是同一轮迭代的预览版，内容已合并到本条。）

### 手机端

- **微信语音输入不再误发**：系统还在自动整理文字时，输入框不会再被当成"已按下发送"，
  把半句话发出去。
- **修复远程界面左侧列表展开后点不动的问题**：此前展开列表后点任何一条都没有反应，
  右侧还会多出一块空白内容挡住界面。
- 会话过期 / 电脑离线时给出可以照做的提示，不再直接显示报错页。
- 修复进入设备后地址栏异常、刷新白屏的问题。

### 远程面板

- **支持深色主题**：面板跟随 Harness 的主题自动切换，不再在深色界面里显示一块白板。
- 悬浮按钮可拖动到任意位置，默认停在右上角，不遮挡内容。
- 邀请入口重做：能得多少会员时长一眼可见。

### 更新与安装

- **"更新中 / 正在安装"不会再长时间无响应**：超过设定时间没有进展会自动切换镜像重试，
  仍然失败则如实提示，而不是一直停在那里。
- 更新过程中可以取消。
- 更新状态可见：当前是稳定版还是预览版、已经进行了多久。
- 修正安装引导：插件市场里的搜索关键词是 `dsh-remote-web`；同时修正了指向旧菜单名称的文案。

### 邀请

- 邀请页改为「带新用户换会员时长」，并写明计入条件（新账号注册 + 电脑端安装并上线）
  与发放对象（仅邀请人）。

## [0.6.8] - 2026-09-19

> 修一个上线几小时内就收到的体验问题：镜像页右下角那颗悬浮按钮**正好压住输入框的发送按钮**。

### Changed

- **悬浮按钮默认挪到右上角，并且可以拖到任何位置**：原来固定在右下角、与发送按钮重叠，手机上
  很容易误触；现在位置会被记住，旋转屏幕、改变窗口宽度后也仍在屏幕内，不会跑到看不见的地方。
- 菜单跟随按钮弹出：按钮在上半屏就挂在下方、在下半屏挂在上方，靠左靠右分别对齐，不会超出屏幕。

### Fixed

- **「按钮点一下没反应、菜单打不开」已修复**：现在轻点即开合菜单，拖动时页面不会跟着滚动。

## [0.6.7] - 2026-09-19

> **正式版**（发表于 npm `latest`）。本轮重点是 **Windows 支持** 与安装 / 卸载的可靠性；
> 0.6.7 的预览版（beta.1 ~ beta.4）内容已全部并入本条。

### Added

- **Windows 支持**：状态查询、启停与自启动不再只按 macOS / Linux 实现；安装后随登录自动启动，
  卸载会一并删除自启动项（登录瞬间可能出现一个控制台窗口，由插件自动接管时不会）。
- **`dsh-remote repair`**：界面起不来时，可在命令行只修复插件挂载，不联网、不动你的运行环境。
- `dsh-remote status` 增加挂载诊断，报障时能一眼看出是不是插件没挂上。

### Changed

- **镜像页的「字号」与「加密小锁」合并成一个悬浮按钮**：点开菜单即可调字号、看加密状态，
  以及**返回设备列表**；手机端和电脑端都能用，也不会被遮罩挡住点不到。

### Fixed

- **Windows 上一批「装得上、用不了」的问题**：面板永远显示「查询中…」或「已停止」、点重启后
  界面起不来、bridge 不会自动拉起、安装中途报错 —— 均已修复。
- **打开镜像页整屏被遮住、界面点不动**（Windows 用户先遇到）：现在只有侧栏真的打开时才拦截点击。
- **卸载后 dsh web 起不来、点卸载让整个 dsh web 卡死**：卸载会先让界面立刻恢复响应，慢的清理
  交给后台执行。
- **安装或升级失败会留下一份起不来的配置**：现在任何一步不通过就中止并如实报错，不会写坏环境。
- **退出登录后手机端仍能访问这台电脑**（安全）：退出登录会同时停掉本机服务并作废设备身份。
- **换账号后新账号看不到这台电脑**：现在会以新账号重新登记，并清掉上一个账号遗留的在线状态；
  「复制诊断信息」也会写明当前用的是哪个账号的身份。
- **点进设备后地址栏只剩端口、刷新可能白屏**：现在地址栏始终显示是哪台设备，刷新与收藏都可用。
- **「过一段时间就报错」**：登录状态与令牌现在同时到期，不会再出现「令牌还有效、页面先失效」。
- **会话过期或电脑离线时不再只甩一张报错页**，改为给出能照做的提示（电脑没在运行会直接说明）。
- **登录被限流却提示「密码错」**：现在会说明是短时间尝试过多、凭据无需修改，并告知何时可重试。
- 面板请求增加超时：网络异常时不再一直转圈，而是给出下一步建议。

## [0.6.6] - 2026-09-16

> **正式版**（发表于 npm `latest`）。0.6.6 的预览版（beta.1 ~ beta.4）内容已全部并入本条。
> 本轮主要修手机端反馈集中的几个问题，并新增手机端字号档位。

### Added

- **手机端字号档位**：右下角「字」按钮 → 滑块，四档（最小 / 中 / 大 / 超大），选择会被记住；
  **只作用于手机端，电脑端完全不受影响**。

### Fixed

- **手机端整屏阴影、点哪都没反应**（只有左侧栏能点）：现在只有侧栏真的滑进来时才拦截点击，
  菜单按钮始终可开可关，点遮罩也能自救。
- **手机端左上角菜单按钮点不开侧栏**：现在以你的操作为准 —— 点了就开、再点就关。
- **扫码后显示「fail to load plugin」**：插件清单里声明了一个并不存在的依赖，已移除。
- **「桌面运行环境缺失」+「一键修复」点了没反应**：现在失败会如实报错；卡住的「进行中」状态
  不会再永久禁止点击。
- **升级后修复到不了手机端**：注入的脚本现在按内容同步，升级后**重启一次 bridge** 即可生效。
- **升级时明明有新版本却提示「已是最新」而不安装**：现在按版本比较，本地更新就装、绝不降级。
- **「刷新后横幅还在」**：只有版本真的变了才提示更新，同一版本被重写多次也不再误报。
- **Windows 上补装失败会每十几秒重试一次**：现在按 30 秒 → 2 分钟 → 10 分钟退避，
  点「一键修复」仍会立即重试。

### Changed

- **旧名别名包 `dsh-remote-ui` 退役**：npm 上已标记 deprecated，`latest` 停在 0.6.5；安装或更新时
  会看到迁移提示 —— 请改用 `@mrrisega/dsh-remote`（插件包 `dsh-remote-web`）。
  安装器仍会自动清理旧名残留，老用户升级不受影响。

## [0.6.5] - 2026-09-13

### Fixed

- **面板「一键更新」在市场装法（npm / 插件市场安装）下不再空转**：此前会显示「安装完成」，
  插件却始终停在旧版本。现在会真的把插件升级到最新，并如实告诉你三种结果之一：已升级
  （x.y.z → x.y.z）/ 已是市场最新版 / 升级失败并给出可手动执行的命令。
- 插件包升级成功后，会明确提示需要**重启一次 dsh web**，否则升级不会生效。

## [0.6.4] - 2026-09-13

> **正式版**。本轮的预览版（beta.1 ~ beta.11）内容已全部并入本条：首次安装体验、macOS 26
> 自启动、插件热加载与匿名使用统计都在这一轮落地。

### Added

- **首次安装不再卡在最后一步**：登录后会自动完成「准备运行环境 → 启动 bridge → 正在连接中继
  → 已连接 ✅」，全程零操作、零刷新；失败会给可读原因、自动重试与一键「复制诊断信息」。
- **手机端空设备列表不再只能干等**：会自动发现电脑上线的设备，不用手动点「刷新」。
- **安装引导不再只有一条终端命令**：没有设备时改成「打开 DeepSeek Harness → 在插件市场搜索
  `dsh-remote` 安装（或复制命令）→ 登录后稍等，电脑会自己出现」三步。
- **插件热加载**：首次安装装完**只需刷新页面，不必重启 dsh web**；同一插件升级到新版本时
  仍需重启一次，安装器会如实提示，不会谎报成功。
- **macOS 26 自启动可用**：修掉「服务登记了却永远不启动」，崩溃后能自动重建；系统确实不支持
  自启动时会如实说明，不再假装完全成功。
- **匿名使用统计可以关闭**：只上报「安装 / 连接是否成功」这类事件，不含账号、会话与文件内容、
  主机名、路径、设备指纹和 IP；`export DSH_REMOTE_TELEMETRY=0` 即完全关闭（[清单](docs/telemetry.md)）。

### Changed

- **装插件与在线更新都不再需要重启 DeepSeek harness**：只有「磁盘上的插件版本与正在运行的版本不同」
  时才提示**刷新页面**，并且不再替你自动重启（刷新零风险，重启会打断你正在进行的会话）。
- 面板底部按钮改为「🔄 重启 dsh web」（仅在刷新后仍看不到面板时才需要）；**安装输出精简**，多阶段重复的提示收敛成结尾一份汇总。

### Fixed

- **插件加载失败导致 dsh web 起不来**：现在只会有一个激活点，冲突时回滚，绝不把能用的配置写坏。
- **手机端偶发白页 / 提示「reopen the URL printed by dsh web」**：现在会自动恢复，不用重启或重新扫码。
- **安装后误报「需要重启 dsh web」**：热加载其实已经生效、或机器繁忙时响应慢一点，都被误判成
  需要重启；现在会如实判断。
- **「刷新后横幅还在」**：只有版本真的变了才提示；装预览版的人也不会被误判成落后于正式版。
- 自启动目录不存在等安装环境问题不再让安装中断；服务也能找到系统命令，不再刷 `command not found`。

## [0.6.3] - 2026-09-12

### Added

- 插件市场详情页不再提示「未声明宿主要求」，并能按 DSH 版本筛选适配的插件。
- 每个版本的更新说明改由 CHANGELOG 自动生成，插件市场里能看到真实条目（此前为空或通用文案）。

### Changed

- 市场的一键安装包改为始终跟随最新版本：此前地址被钉在 v0.6.0，导致市场长期装到旧版。

## [0.6.2] - 2026-09-11

> 稳定版（`latest`）。主题：**插件市场安装即可用**。

### Added

- **首次安装 / 更新后的重启引导**：面板顶部醒目提示并带「重启」按钮，底部另有常驻入口；
  重启期间页面会自动轮询，服务恢复后自动刷新，不需要你手动操作。
  （这项引导自 0.6.4 起已不再需要 —— 那一版做到了改完不必重启。）

### Fixed

- **只装插件（插件市场 / `dsh plugin add`）时 bridge 起不来、并且永远不自愈**：桌面运行环境
  从未被补齐，而启动项指向一个不存在的脚本，于是反复崩溃重启。现在缺环境会先补齐再启动，
  不会再生成指向空路径的自启动项；崩溃循环也会如实显示为「启动失败（已自动转入修复）」，
  而不是谎报「运行中」。

## [0.6.1] - 2026-09-10

> 稳定版（`latest`）。

### Added

- **交流群二维码**：管理后台「推广」页可上传，官网落地页、付费页、电脑端「设置 → 远程访问 →
  关于 dsh-remote」的「💬 加入交流群」按钮与用户反馈页都会展示；未上传时不显示入口，
  图片加载失败也会自动隐藏。README 也增加了交流群章节。

### Fixed

- **首次安装后立即登录，二维码与设备列表报「尚未登录」红字**（要刷新页面才恢复）：只装插件的
  路径缺少中继密钥，现在会自动补齐并缓存，服务端轮换密钥后也会自动重取。
- **面板不再需要手动刷新页面**：登录成功（含换账号）与 bridge 启动 / 重启都会自动刷新二维码与
  设备列表；中继短暂不可用会自动重试，成功后提示自动消失，红字旁常驻「立即重试」。

### Changed

- 错误提示不再一律谎报「尚未登录」，改为区分「未登录（去登录）」「本机密码已失效」
  「中继未就绪或不可达」，并保留原因便于排查。

## [0.6.0] - 2026-09-09

> 本版只记录**面向用户**的变化（管理后台/服务端后台更新不在此列）。稳定通道仍为
> `latest`（0.5.0），本版走 `beta` 预发通道。

### Added

- **扫码/无密码路径免输 E2EE 密码（桌面授权引入）**：用电脑端二维码或一次性链接进入后，
  由电脑自动授权开启端到端加密；短信/扫码登录不再要求“再输一次密码”。
- **记住本机（刷新不掉回明文）**：解锁一次后派生密钥安全保存于本机浏览器，手机 App 刷新、
  镜像页刷新都自动恢复加密；退出登录即清除（浏览器无痕可不用）。
- 电脑端侧栏新增 **「📱 远程访问」快捷按钮**（与官方「设置」共存、不遮挡），首次带引导红点。

### Changed

- 移动端体验：选中会话后侧栏抽屉自动收起；E2EE 状态徽标展示几秒后自动缩成小 🔒。
- 修复移动端“Settings are unavailable”/模型提供方目录加载失败（设置页在镜像环境可正常使用）。

### Security

- E2EE 依旧：服务端只保存密码的不可逆校验值，不存密码明文与内容；“记住本机”将派生密钥
  存于本机浏览器（等同网页端“记住密码”的暴露面），退出登录即清除——详见
  `docs/e2ee-protocol.md` §5.1/§5.4。

## [0.5.0] - 2026-09-08

### Changed

- **Plugin renamed `dsh-remote-ui` → `dsh-remote-web`** (plugin directory
  `packages/dsh-remote-web`, npm `dsh-remote-web`): the package is dsh-remote's
  dsh web plugin half (remote-control host plugin + settings panel), so `-web`
  matches what it is; `-ui` read as a pure UI/skin plugin. Installers and the
  panel's self-uninstall now migrate/clean the legacy name (≤0.4.9
  `dsh-remote-ui`, `dsh-remote-ui-plugin`) alongside the new one, so upgrades
  and uninstalls leave no double-activation residue.
- The root CLI is bumped to 0.5.0 to ship the renamed plugin together with that
  migration, so upgrade and uninstall paths no longer reference the old name.

## [0.3.1] - 2026-08-30

### Changed

- Feedback captcha is now optional: anonymous users (including self-hosted
  deployments without a phone account) can submit feedback and satisfaction
  ratings directly; the server still records the device IP and enforces
  per-identity/IP/global rate limits. A captcha is validated when provided.
- Payment modal shows a prominent announcement block from the server config
  (`upgrade_announcement`, `{wechat}` substituted with the configured WeChat
  id); admin console textarea enlarged for editing it.
- README: added screenshot gallery (phone mirror view, device list,
  self-hosted settings).

## [0.3.0] - 2026-08-28

### Changed

- **Project renamed to `dsh-remote`** — package, binaries, docs and commands
  now use `dsh-remote` (`npx @mrrisega/dsh-remote`). The old npm package
  `@mrrisega/dsh-relay` is deprecated.

### Fixed

- Feedback: logged-in users (SaaS account or self-hosted key) no longer need a
  captcha — the plugin's node half attaches the account JWT automatically, and
  the panel/popup hide the captcha field accordingly. Anonymous submissions
  keep the captcha requirement.
- Satisfaction popup: submit no longer blocks on a hidden captcha when
  anonymous users cannot load it; captcha is loaded on demand and errors are
  actionable.

## [0.2.1] - 2026-08-28

### Fixed

- `setup` / settings page now fetch the server-issued `bridge_secret` from
  `public-config` automatically, so a fresh one-command install can sign in to
  the cloud service without manual configuration.
- Bridge no longer misreports `device_limit_exceeded` (409) as "device bound to
  another account"; it now prints the server's actual message with guidance to
  remove the old device first.

## [0.2.0] - 2026-08-27

First public release of the tunnel-mode architecture.

### Added

- One-command client install via npm: `npx @mrrisega/dsh-remote`
  (installs bridge + dsh web plugin + autostart in one shot; login happens in
  the local settings page afterwards).
  Self-hosted mode: `npx @mrrisega/dsh-remote setup --server wss://… --key …`.
- Self-hosted mode: `relay-router` local authentication
  (`DSH_LOCAL_ACCESS_KEYS` → `POST /_login` → short-lived local JWT),
  with zero dependency on the closed-source account system.
- Router: real-time device list (`GET /_devices`), quota status (`GET /_quota`),
  per-plan token-bucket bandwidth limiting and monthly traffic caps, gzip
  pass-through for tunneled responses.
- Bridge: tunnel-only daemon with heartbeat-based half-open detection and
  exponential-backoff reconnect; gzip compression for compressible upstream
  responses.
- dsh web plugin (`dsh-remote-ui`): settings-page panel with connection-mode
  switching (cloud / self-hosted), account login & registration, invite links,
  plan status, user feedback card, and bridge lifecycle control.
- Docker deployment for self-hosting: `Dockerfile` + `docker-compose.yml`.

### Removed

- WebRTC / peer-to-peer / signaling / STUN / TURN stack (and all related
  packages and endpoints) — replaced by the tunnel-mode architecture.

## License

PolyForm Noncommercial 1.0.0 — free for personal, research and non-commercial
use; commercial use requires a license (see `COMMERCIAL-LICENSE.md`).
